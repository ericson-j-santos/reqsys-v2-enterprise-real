name: BACEN Subprocessor Readiness

on:
  pull_request:
    branches: [main]
    paths:
      - "governance/bacen/VENDOR-DPA-READINESS.yaml"
      - "scripts/generate_bacen_subprocessor_readiness.py"
      - ".github/workflows/bacen-subprocessor-readiness.yml"
  push:
    branches: [main]
    paths:
      - "governance/bacen/VENDOR-DPA-READINESS.yaml"
      - "scripts/generate_bacen_subprocessor_readiness.py"
  schedule:
    - cron: "17 8 1 * *"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --disable-pip-version-check PyYAML
      - run: python -m py_compile scripts/generate_bacen_subprocessor_readiness.py
      - run: python scripts/generate_bacen_subprocessor_readiness.py --manifest governance/bacen/VENDOR-DPA-READINESS.yaml --output artifacts/bacen/bacen-05-subprocessor-readiness.json
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bacen-05-subprocessor-readiness
          path: artifacts/bacen/bacen-05-subprocessor-readiness.json
          if-no-files-found: error
          retention-days: 365
