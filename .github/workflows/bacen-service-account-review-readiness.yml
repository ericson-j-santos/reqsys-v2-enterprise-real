name: BACEN Service Account Review Readiness

on:
  pull_request:
    branches: [main]
    paths:
      - "governance/bacen/ACCESS-REVIEW-REGISTER.yaml"
      - "scripts/generate_bacen_service_account_review_readiness.py"
      - ".github/workflows/bacen-service-account-review-readiness.yml"
  push:
    branches: [main]
    paths:
      - "governance/bacen/ACCESS-REVIEW-REGISTER.yaml"
      - "scripts/generate_bacen_service_account_review_readiness.py"
      - ".github/workflows/bacen-service-account-review-readiness.yml"
  schedule:
    - cron: "7 9 1 * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: bacen-service-account-review-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: BACEN-02 Service Account Review
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check PyYAML

      - name: Validate implementation
        run: python -m py_compile scripts/generate_bacen_service_account_review_readiness.py

      - name: Generate readiness evidence
        run: |
          python scripts/generate_bacen_service_account_review_readiness.py \
            --register governance/bacen/ACCESS-REVIEW-REGISTER.yaml \
            --output artifacts/bacen/bacen-02-service-account-review-readiness.json

      - name: Validate evidence contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          path = Path("artifacts/bacen/bacen-02-service-account-review-readiness.json")
          evidence = json.loads(path.read_text(encoding="utf-8"))
          assert evidence["control_id"] == "BACEN-02"
          assert evidence["structural_checks_passed"] is True
          assert evidence["service_accounts_in_scope"] is True
          assert evidence["coverage_record_present"] is False
          assert evidence["readiness_status"] == "pending"
          assert evidence["production_touched"] is False
          assert len(evidence["register_sha256"]) == 64
          print("BACEN-02 service account review evidence: valid")
          PY

      - name: Publish evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bacen-02-service-account-review-readiness
          path: artifacts/bacen/bacen-02-service-account-review-readiness.json
          if-no-files-found: error
          retention-days: 365

      - name: Publish summary
        if: always()
        run: |
          python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          from pathlib import Path

          path = Path("artifacts/bacen/bacen-02-service-account-review-readiness.json")
          print("## BACEN-02 — Revisão de contas de serviço")
          if not path.exists():
              print("\n- Evidência não gerada.")
              raise SystemExit(1)
          evidence = json.loads(path.read_text(encoding="utf-8"))
          print(f"\n- Contas de serviço no escopo: **{evidence['service_accounts_in_scope']}**")
          print(f"- Registro de cobertura presente: **{evidence['coverage_record_present']}**")
          print(f"- Cobertura completa: **{evidence['coverage']['complete']}**")
          print(f"- Integridade estrutural: **{evidence['structural_checks_passed']}**")
          print(f"- Prontidão: **{evidence['readiness_status']}**")
          print("- Produção acessada: **false**")
          PY
