name: STG Blocking Policy Authorization

on:
  pull_request:
    paths:
      - "docs/contracts/environment-promotion-policy-v1.json"
      - "docs/contracts/stg-blocking-change-authorization.json"
      - "docs/contracts/stg-enforcement-approval.md"
      - "scripts/build_stg_enforcement_approval.py"
      - "scripts/validate_stg_policy_approval.py"
      - "tests/test_build_stg_enforcement_approval.py"
      - "tests/test_validate_stg_policy_approval.py"
      - ".github/workflows/stg-enforcement-approval.yml"
      - ".github/workflows/stg-blocking-policy-authorization.yml"
  workflow_dispatch:
    inputs:
      policy_pr_number:
        description: Pull request containing the STG blocking policy change
        required: true
        type: string

permissions:
  actions: read
  contents: read
  pull-requests: read

concurrency:
  group: stg-blocking-policy-authorization-${{ github.event.pull_request.number || inputs.policy_pr_number || github.ref }}
  cancel-in-progress: true

jobs:
  authorize:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install focused test dependencies
        run: python -m pip install --disable-pip-version-check "pytest>=8.2,<9"

      - name: Run focused tests
        run: python -m pytest -q tests/test_build_stg_enforcement_approval.py tests/test_validate_stg_policy_approval.py

      - name: Resolve policy pull request head SHA
        id: policy
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_PR_NUMBER: ${{ github.event.pull_request.number }}
          INPUT_PR_NUMBER: ${{ inputs.policy_pr_number }}
        run: |
          set -euo pipefail
          pr_number="${EVENT_PR_NUMBER:-${INPUT_PR_NUMBER:-}}"
          case "$pr_number" in
            ''|*[!0-9]*) echo "::error::A numeric policy PR number is required"; exit 1 ;;
          esac

          pr_json="$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${pr_number}")"
          state="$(jq -r '.state' <<<"$pr_json")"
          merged="$(jq -r '.merged' <<<"$pr_json")"
          sha="$(jq -r '.head.sha // empty' <<<"$pr_json")"
          base_sha="$(jq -r '.base.sha // empty' <<<"$pr_json")"
          base_ref="$(jq -r '.base.ref // empty' <<<"$pr_json")"
          head_ref="$(jq -r '.head.ref // empty' <<<"$pr_json")"

          if [ "$state" != "open" ] && [ "$merged" != "true" ]; then
            echo "::error::Policy PR #${pr_number} must be open or merged; state=${state}, merged=${merged}"
            exit 1
          fi
          [[ "$sha" =~ ^[0-9a-fA-F]{40}$ ]] || {
            echo "::error::Unable to resolve full head SHA for policy PR #${pr_number}"
            exit 1
          }

          echo "number=$pr_number" >> "$GITHUB_OUTPUT"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"
          echo "state=$state" >> "$GITHUB_OUTPUT"
          echo "merged=$merged" >> "$GITHUB_OUTPUT"
          echo "base_sha=$base_sha" >> "$GITHUB_OUTPUT"
          echo "base_ref=$base_ref" >> "$GITHUB_OUTPUT"
          echo "head_ref=$head_ref" >> "$GITHUB_OUTPUT"

      - name: Resolve one-time authorization scope
        id: scope
        env:
          GH_TOKEN: ${{ github.token }}
          POLICY_PR: ${{ steps.policy.outputs.number }}
          BASE_SHA: ${{ steps.policy.outputs.base_sha }}
          BASE_REF: ${{ steps.policy.outputs.base_ref }}
          HEAD_REF: ${{ steps.policy.outputs.head_ref }}
        run: |
          set -euo pipefail
          scope="policy_change"
          transition="$(jq -c '.exception_retirement_transition' docs/contracts/stg-blocking-change-authorization.json)"
          expected_reference="$(jq -r '.reference // empty' <<<"$transition")"
          expected_scope="$(jq -r '.approval_scope // empty' <<<"$transition")"
          expected_base_sha="$(jq -r '.required_base_sha // empty' <<<"$transition")"
          expected_head_ref="$(jq -r '.required_head_ref // empty' <<<"$transition")"
          reusable="$(jq -r '.reusable' <<<"$transition")"

          transition_contract_valid=false
          if [ "$expected_reference" = "GH-RUN-32615688406" ] \
            && [ "$expected_scope" = "exception_retirement" ] \
            && [ "$expected_base_sha" = "4dd44b064b6099eba78a88ad84d7e465345e11dd" ] \
            && [ "$expected_head_ref" = "hotfix/retire-stg-temporary-exception" ] \
            && [ "$reusable" = "false" ]; then
            transition_contract_valid=true
          fi

          if [ "$transition_contract_valid" = "true" ] \
            && [ "$BASE_REF" = "main" ] \
            && [ "$BASE_SHA" = "$expected_base_sha" ] \
            && [ "$HEAD_REF" = "$expected_head_ref" ]; then
            mapfile -t changed_files < <(
              gh api --paginate --slurp "repos/${GITHUB_REPOSITORY}/pulls/${POLICY_PR}/files?per_page=100" \
                --jq '.[][] | .filename'
            )
            allowed_files=(
              ".github/workflows/governed-pr-sync.yml"
              ".github/workflows/stg-blocking-policy-authorization.yml"
              ".github/workflows/stg-enforcement-approval.yml"
              ".github/workflows/stg-post-merge-validation.yml"
              ".github/workflows/stg-temporary-exception-retirement.yml"
              ".github/workflows/workflow-regression-contracts.yml"
              "AGENTS.md"
              "artifacts/agent-increment-gate/agent-increment-gate.json"
              "docs/contracts/stg-blocking-change-authorization.json"
              "docs/contracts/stg-enforcement-approval.md"
              "scripts/build_stg_enforcement_approval.py"
              "scripts/validate_stg_policy_approval.py"
              "scripts/validate_workflow_regression_contracts.py"
              "tests/test_build_stg_enforcement_approval.py"
              "tests/test_validate_stg_policy_approval.py"
            )
            required_files=(
              ".github/workflows/stg-blocking-policy-authorization.yml"
              ".github/workflows/stg-enforcement-approval.yml"
              ".github/workflows/stg-temporary-exception-retirement.yml"
              "docs/contracts/stg-blocking-change-authorization.json"
              "scripts/build_stg_enforcement_approval.py"
              "scripts/validate_stg_policy_approval.py"
            )
            declare -A allowed=()
            declare -A changed=()
            retirement_files_valid=true
            for path in "${allowed_files[@]}"; do allowed["$path"]=1; done
            for path in "${changed_files[@]}"; do
              changed["$path"]=1
              if [ -z "${allowed[$path]+x}" ]; then retirement_files_valid=false; fi
            done
            for path in "${required_files[@]}"; do
              if [ -z "${changed[$path]+x}" ]; then retirement_files_valid=false; fi
            done
            if [ "$retirement_files_valid" = "true" ]; then
              scope="exception_retirement"
            fi
          fi

          echo "scope=$scope" >> "$GITHUB_OUTPUT"

      - name: Discover human approval artifact for current PR SHA
        id: approval
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SHA: ${{ steps.policy.outputs.sha }}
          POLICY_PR: ${{ steps.policy.outputs.number }}
          EXPECTED_SCOPE: ${{ steps.scope.outputs.scope }}
        run: |
          set -euo pipefail
          mkdir -p .tmp/candidates .tmp/approval artifacts/stg-policy-approval-validation
          artifact_name="$(jq -r '.approval_artifact_name' docs/contracts/stg-blocking-change-authorization.json)"
          test -n "$artifact_name"

          gh api --paginate "repos/${GITHUB_REPOSITORY}/actions/artifacts?name=${artifact_name}&per_page=100" \
            --jq '.artifacts | sort_by(.created_at) | reverse | .[] | select(.expired == false) | [.id, .workflow_run.id] | @tsv' \
            > .tmp/candidates/artifacts.tsv

          found=""
          while IFS=$'\t' read -r artifact_id run_id; do
            [ -n "$artifact_id" ] && [ -n "$run_id" ] || continue
            run_json="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${run_id}" 2>/dev/null)" || continue
            run_name="$(jq -r '.name // empty' <<<"$run_json")"
            run_conclusion="$(jq -r '.conclusion // empty' <<<"$run_json")"
            if [ "$run_name" != "STG Enforcement Approval" ] || [ "$run_conclusion" != "success" ]; then
              continue
            fi

            candidate_dir=".tmp/candidates/${artifact_id}"
            mkdir -p "$candidate_dir"
            gh api "repos/${GITHUB_REPOSITORY}/actions/artifacts/${artifact_id}/zip" \
              > "$candidate_dir/artifact.zip" 2>/dev/null || continue
            unzip -q "$candidate_dir/artifact.zip" -d "$candidate_dir/content" || continue
            candidate="$(find "$candidate_dir/content" -name approval.json -print -quit)"
            [ -n "$candidate" ] || continue

            if python scripts/validate_stg_policy_approval.py \
              --approval "$candidate" \
              --expected-sha "$TARGET_SHA" \
              --expected-pr-number "$POLICY_PR" \
              --expected-scope "$EXPECTED_SCOPE" \
              --output "$candidate_dir/validation.json"
            then
              cp "$candidate" .tmp/approval/approval.json
              echo "artifact_id=$artifact_id" >> "$GITHUB_OUTPUT"
              echo "run_id=$run_id" >> "$GITHUB_OUTPUT"
              echo "mode=strict_evidence_only" >> "$GITHUB_OUTPUT"
              found=true
              break
            fi
          done < .tmp/candidates/artifacts.tsv

          if [ "$found" != "true" ]; then
            printf '{}\n' > .tmp/approval/approval.json
            echo "artifact_id=not-found" >> "$GITHUB_OUTPUT"
            echo "run_id=not-found" >> "$GITHUB_OUTPUT"
            echo "mode=strict_evidence_only" >> "$GITHUB_OUTPUT"
          fi

      - name: Validate approval artifact
        env:
          POLICY_SHA: ${{ steps.policy.outputs.sha }}
          POLICY_PR: ${{ steps.policy.outputs.number }}
          EXPECTED_SCOPE: ${{ steps.scope.outputs.scope }}
        run: |
          python scripts/validate_stg_policy_approval.py \
            --approval .tmp/approval/approval.json \
            --expected-sha "$POLICY_SHA" \
            --expected-pr-number "$POLICY_PR" \
            --expected-scope "$EXPECTED_SCOPE" \
            --output artifacts/stg-policy-approval-validation/report.json

      - name: Publish authorization summary
        if: always()
        env:
          POLICY_PR: ${{ steps.policy.outputs.number }}
          POLICY_SHA: ${{ steps.policy.outputs.sha }}
          POLICY_STATE: ${{ steps.policy.outputs.state }}
          POLICY_MERGED: ${{ steps.policy.outputs.merged }}
          APPROVAL_RUN_ID: ${{ steps.approval.outputs.run_id }}
          APPROVAL_ARTIFACT_ID: ${{ steps.approval.outputs.artifact_id }}
          RESOLUTION_MODE: ${{ steps.approval.outputs.mode }}
          AUTHORIZATION_SCOPE: ${{ steps.scope.outputs.scope }}
        run: |
          {
            echo "# STG Blocking Policy Authorization"
            echo
            echo "- Policy PR: **#${POLICY_PR:-unresolved}**"
            echo "- Policy SHA: \`${POLICY_SHA:-unresolved}\`"
            echo "- Policy state: **${POLICY_STATE:-unresolved}**"
            echo "- Policy merged: **${POLICY_MERGED:-unresolved}**"
            echo "- Human approval run ID: **${APPROVAL_RUN_ID:-not found}**"
            echo "- Approval artifact ID: **${APPROVAL_ARTIFACT_ID:-not found}**"
            echo "- Resolution mode: **${RESOLUTION_MODE:-strict_evidence_only}**"
            echo "- Authorization scope: **${AUTHORIZATION_SCOPE:-unresolved}**"
            echo "- Automatic deploy: **disabled**"
            echo "- Automatic policy change: **disabled**"
            echo "- Fail closed without valid evidence: **enabled**"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Publish audit artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stg-policy-approval-validation
          path: artifacts/stg-policy-approval-validation/report.json
          retention-days: 365
