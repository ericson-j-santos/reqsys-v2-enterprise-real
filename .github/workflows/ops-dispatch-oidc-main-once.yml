name: Ops dispatch OIDC main once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dispatcher temporário desativado após execução bem-sucedida."
