# Governed YAML validation fix

Registro de correção do validador YAML governado.

- Parser YAML determinístico no runner.
- Evidência publicada com `if: always()`.
- Sem deploy, produção, secrets ou permissões.
