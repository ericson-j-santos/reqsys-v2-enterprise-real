# Prompts — SQL Visual Explain

## Explicação objetiva

Explique a consulta SQL abaixo seguindo o fluxo Contexto → Missão → Análise → Query → Resultado. Identifique tabelas, joins, filtros, agregações, ordenação, riscos e consultas derivadas úteis.

```sql
-- cole a query aqui
```

## Revisão enterprise

Revise a consulta SQL abaixo como arquiteto de dados. Aponte riscos de performance, ambiguidade de regra, problemas de join, filtros potencialmente incorretos, necessidade de índice, testes recomendados e documentação Mermaid/ERD necessária.

```sql
-- cole a query aqui
```

## Documentação viva

Gere documentação Markdown para a consulta SQL abaixo contendo: objetivo de negócio, tabelas, joins, filtros, métricas, regras, premissas, riscos, exemplos de validação, Mermaid ERD e próximos incrementos.

```sql
-- cole a query aqui
```
