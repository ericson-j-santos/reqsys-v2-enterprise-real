# Upstreams do ReqSys.
# Ajustar nomes conforme docker compose, Kubernetes service ou plataforma de deploy.

upstream reqsys_backend {
    server reqsys-backend:8000;
    keepalive 32;
}

upstream reqsys_frontend {
    server reqsys-frontend:3000;
    keepalive 32;
}
