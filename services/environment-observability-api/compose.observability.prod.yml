services:
  api:
    build: .
    environment:
      APP_ENV: production
      OTEL_TRACING_ENABLED: "true"
      OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: http://collector:4318/v1/traces
    ports:
      - "127.0.0.1:28000:8000"
    depends_on:
      collector:
        condition: service_healthy
    networks: [observability, host-access]

  collector-queue-init:
    image: alpine:3.22
    command:
      - /bin/sh
      - -ec
      - |
        mkdir -p /var/lib/otelcol/queue
        chown -R 10001:10001 /var/lib/otelcol
        chmod 0750 /var/lib/otelcol /var/lib/otelcol/queue
    user: "0:0"
    volumes:
      - collector-queue-prod:/var/lib/otelcol
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    cap_add: ["CHOWN", "DAC_OVERRIDE", "FOWNER"]
    networks: [observability]

  collector:
    image: otel/opentelemetry-collector-contrib:0.128.0
    user: "10001:10001"
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    environment:
      APP_ENV: production
      OTEL_UPSTREAM_ENDPOINT: ${PROD_OTEL_UPSTREAM_ENDPOINT:?required}
      OTEL_UPSTREAM_AUTHORIZATION: ${PROD_OTEL_UPSTREAM_AUTHORIZATION:-}
    volumes:
      - ./collector/config.yaml:/etc/otelcol-contrib/config.yaml:ro
      - collector-queue-prod:/var/lib/otelcol
    ports:
      - "127.0.0.1:28133:13133"
      - "127.0.0.1:28888:8888"
    depends_on:
      collector-queue-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "/otelcol-contrib", "validate", "--config=/etc/otelcol-contrib/config.yaml"]
      interval: 10s
      timeout: 5s
      retries: 18
      start_period: 10s
    read_only: true
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    networks: [observability, host-access]

  prometheus:
    image: prom/prometheus:v3.4.1
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=30d
    volumes:
      - ./observability/prod/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./observability/prometheus-alerts.yaml:/etc/prometheus/rules/alerts.yaml:ro
      - prometheus-data-prod:/prometheus
    ports:
      - "127.0.0.1:29090:9090"
    depends_on: [api, collector, alertmanager]
    networks: [observability, host-access]

  alertmanager:
    image: prom/alertmanager:v0.28.1
    command: ["--config.file=/etc/alertmanager/alertmanager.yml"]
    volumes:
      - ./observability/prod/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    ports:
      - "127.0.0.1:29093:9093"
    networks: [observability, host-access]

  grafana:
    image: grafana/grafana:12.0.1
    environment:
      GF_SECURITY_ADMIN_USER: ${PROD_GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${PROD_GRAFANA_ADMIN_PASSWORD:?required}
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - ./observability/dev/grafana-provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana-dashboard.json:/var/lib/grafana/dashboards/environment-observability.json:ro
      - grafana-data-prod:/var/lib/grafana
    ports:
      - "127.0.0.1:23000:3000"
    depends_on: [prometheus]
    networks: [observability, host-access]

networks:
  observability:
    internal: true
  host-access:

volumes:
  collector-queue-prod:
  prometheus-data-prod:
  grafana-data-prod:
